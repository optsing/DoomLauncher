<ContentDialog
    x:Class="DoomLauncher.PackageSelectorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DoomLauncher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DefaultButton="Primary"
    Title="Выберите версию"
    PrimaryButtonText="Установить"
    CloseButtonText="Отмена"
    Style="{ThemeResource DefaultContentDialogStyle}"
>

    <ListView SelectedItem="{x:Bind SelectedPackage, Mode=TwoWay}" ItemsSource="{x:Bind OnlinePackage}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="local:GZDoomPackage">
                <TextBlock Text="{x:Bind local:SettingsPage.GZDoomPackageToTitle((local:GZDoomPackage))}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentDialog>
