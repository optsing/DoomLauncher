<Page
    x:Class="DoomLauncher.DoomPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DoomLauncher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    d:DataContext="{d:DesignInstance Type=local:DoomEntry}"
    mc:Ignorable="d"
    Background="Transparent"
>
    <Grid RowDefinitions="Auto,*">
        <Image
            Grid.RowSpan="2"
            Source="{x:Bind local:DoomPage.GetCurrentBackground(entry.ImageFiles), Mode=OneWay}"
            Opacity="0.25"
            Stretch="UniformToFill"
        />
        <StackPanel
            Grid.RowSpan="2"
            Orientation="Horizontal"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Visibility="{x:Bind local:DoomPage.HasNoItems(entry.ModFiles.Count), Mode=OneWay}"
        >
            <TextBlock
                FontSize="24"
            >Перетащите или выберите файлы</TextBlock>
            <FontIcon Glyph="&#xF0AD;" Margin="8,4,0,0" FontSize="24" VerticalAlignment="Center" />
        </StackPanel>
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto,Auto,Auto">
            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Margin="16,0"
                Text="{x:Bind entry.Name, Mode=OneWay}"
                FontSize="18"
                Style="{ThemeResource BaseTextBlockStyle}"
                TextWrapping="NoWrap"
            />
            <Button Grid.Column="1" Margin="4,8" Click="Append_Click">
                <StackPanel Orientation="Horizontal">
                    <SymbolIcon Symbol="Add" Height="18" Margin="0,0,4,0"/>
                    <TextBlock>Добавить файлы</TextBlock>
                </StackPanel>
            </Button>
            <Button Grid.Column="2" Margin="4,8" VerticalAlignment="Center">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Изменить фон" Click="Background_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Pictures" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Убрать фон" Click="RemoveBackground_Click" IsEnabled="{x:Bind local:DoomPage.HasItems(entry.ImageFiles.Count), Mode=OneWay}"/>
                        <MenuFlyoutSeparator />
                        <MenuFlyoutItem Text="Настроить сборку" Click="EditMod_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Edit" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Удалить сборку" Click="RemoveMod_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Delete" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Button.Flyout>
                <StackPanel Orientation="Horizontal">
                    <AnimatedIcon Height="18" Margin="0,0,4,0">
                        <AnimatedIcon.Source>
                            <animatedvisuals:AnimatedSettingsVisualSource />
                        </AnimatedIcon.Source>
                        <AnimatedIcon.FallbackIconSource>
                            <SymbolIconSource Symbol="Setting"/>
                        </AnimatedIcon.FallbackIconSource>
                    </AnimatedIcon>
                    <TextBlock>Параметры</TextBlock>
                </StackPanel>
            </Button>
            <Button Grid.Column="3" Width="180" Margin="4,8,8,8" VerticalAlignment="Center" Click="Start_Click" Style="{ThemeResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE7FC;" Margin="0,0,4,0" />
                    <TextBlock>Играть</TextBlock>
                </StackPanel>
            </Button>
        </Grid>
        <ListView 
            x:Name="lwModFiles"
            Grid.Row="1"
            SelectionMode="None"
            CanReorderItems="True"
            AllowDrop="True"
            ItemsSource="{x:Bind entry.ModFiles}"
            IsMultiSelectCheckBoxEnabled="False"
            DragOver="LwModFiles_DragOver"
            Drop="LwModFiles_Drop"
        >
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:NamePath">
                    <Grid ColumnDefinitions="*,Auto,Auto">
                        <TextBlock
                            Text="{x:Bind Name}"
                            VerticalAlignment="Center"
                        />
                        <Button
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            Click="OpenContainFolder_Click"
                            Style="{ThemeResource EllipsisButton}"
                            Height="40"
                            Width="40"
                            ToolTipService.ToolTip="Открыть расположение файла"
                        >
                            <FontIcon Glyph="&#xE838;" FontSize="16" />
                        </Button>
                        <Button
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            Click="RemoveFile_Click"
                            Style="{ThemeResource EllipsisButton}"
                            Height="40"
                            Width="40"
                            ToolTipService.ToolTip="Удалить ссылку на файл"
                        >
                            <FontIcon Glyph="&#xE74D;" FontSize="16" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
