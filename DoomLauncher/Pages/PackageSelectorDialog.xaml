<ContentDialog
    x:Class="DoomLauncher.PackageSelectorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:DoomLauncher"
    mc:Ignorable="d"
    DefaultButton="Primary"
    x:Uid="PackageSelectorDialog"
    Style="{ThemeResource DefaultContentDialogStyle}"
>
    <ListView SelectedItem="{x:Bind SelectedPackage, Mode=TwoWay}" ItemsSource="{x:Bind OnlinePackage}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="local:DoomPackageViewModel">
                <TextBlock Text="{x:Bind Title}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentDialog>
